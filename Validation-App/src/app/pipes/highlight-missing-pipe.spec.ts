import { HighlightMissingPipe } from './highlight-missing-pipe';

describe('HighlightMissingPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightMissingPipe();
    expect(pipe).toBeTruthy();
  });
});
